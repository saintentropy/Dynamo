<UserControl x:Class="NodeIcon.NodeIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Name="NodeIconWindowUserControl">
    <Grid x:Name="IconGrid" HorizontalAlignment="Left" VerticalAlignment="Top" 
          IsHitTestVisible="False"
          Visibility="{Binding Path=Visible}">
        <Border Background="#FF353535">
            <Border.OpacityMask>
                <VisualBrush>
                    <VisualBrush.Visual>
                        <Border CornerRadius="20"  Height="125" Width="125" Background="#FF353535"/>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Border.OpacityMask>
        </Border>
        <Image Name="Icon"
               Height="110"
               Width="110"
               Canvas.ZIndex="100"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Source="{Binding Path=LargeIcon}"
               Visibility="Visible"
               Margin="5">
        </Image>
    </Grid>
</UserControl>